version: "2"
sql:
- schema: "sql/schema.sql"
  queries: "sql/query.sql"
  engine: "sqlite"
  gen:
    go: 
      package: "db"
      out: "./db"
      emit_json_tags: true
      emit_prepared_queries: true